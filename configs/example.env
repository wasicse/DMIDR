# Copy this file to configs/local.env and fill in the required values.
# Then run:
#   bash run_all.sh [configs/local.env]

# ── Required ───────────────────────────────────────────────────────────────────

BLAST_DB=/absolute/path/to/blast/database/nr
ESMDISPRED_IMAGE=wasicse/esmdispred:latest
LARGE_MODELS_DIR=/absolute/path/to/DMIDR/scripts/tools/ESMDisPred/largeModels

# ── Optional overrides (defaults shown) ───────────────────────────────────────

INPUT_FASTA=/absolute/path/to/DMIDR/data/input/sequences/example.fasta
MAX_MUTATIONS=5

# Mutant filtering — raise these to generate fewer, higher-confidence mutants
# MIN_DISORDER_PROB: residue must have disorder score above this (0.0–1.0, default 0.5)
# REQUIRED_RATIO:    fraction of block residues that must pass the above threshold (default 0.8)
MIN_DISORDER_PROB=0.5
REQUIRED_RATIO=0.8

# ESMDisPred model: 1=ESMDisPred-1  2=ESMDisPred-2  3=ESMDisPred-2PDB  4=ESMDisPred-DNN
MODEL=3
